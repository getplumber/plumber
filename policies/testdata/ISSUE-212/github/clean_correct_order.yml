# contains(github.ref, 'refs/heads/main') — ref is the haystack,
# branch the needle. Expected: 0 findings.
name: Deploy
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: contains(github.ref, 'refs/heads/main')
    steps:
      - run: ./deploy.sh
