# Explicit set via fromJSON — canonical safe form for a list of
# allowed values. Expected: 0 findings.
name: Deploy
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: contains(fromJSON('["main", "release", "staging"]'), github.ref_name)
    steps:
      - run: ./deploy.sh
