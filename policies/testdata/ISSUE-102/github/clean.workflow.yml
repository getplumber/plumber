# GitHub Actions workflow — all container tags pinned to specific versions.
# Expected: 0 findings.
name: CI
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    container: alpine:3.19
    steps:
      - run: echo build

  deploy:
    runs-on: ubuntu-latest
    container:
      image: node:20.10.0
    steps:
      - run: echo deploy
