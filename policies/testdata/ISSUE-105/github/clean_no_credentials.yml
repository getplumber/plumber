# Public image — no credentials block at all. Expected: 0 findings.
name: Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - run: echo build
