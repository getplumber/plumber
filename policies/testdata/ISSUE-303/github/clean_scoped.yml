# Individual secrets named directly — GitHub still knows every value
# and masks logs. Expected: 0 findings.
name: Deploy
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      API_TOKEN: ${{ secrets.API_TOKEN }}
      API_USER: ${{ secrets.API_USER }}
    steps:
      - run: ./deploy.sh
