# Writing a literal string into $GITHUB_ENV is fine — no user input
# involved. Expected: 0 findings.
name: Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "BUILD_MODE=release" >> $GITHUB_ENV
      - run: echo "/usr/local/custom/bin" >> $GITHUB_PATH
